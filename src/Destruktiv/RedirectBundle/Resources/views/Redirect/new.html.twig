{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Redirect creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('redirect') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
