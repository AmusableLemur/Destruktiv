{% extends "::base.html.twig" %}

{% block body %}
    <ul class="breadcrumb">
        <li>Spel</li>
        <li>Valvet</li>
        <li class="active">Admin</li>
    </ul>

    <h1>
        <a href="{{ path("vault") }}" class="btn btn-default pull-right">
            <i class="fa fa-chevron-left fa-fw"></i>
            Tillbaka
        </a>
        Välkommen till valvet
    </h1>

    {% for level in levels %}
        {{ level.level }}
    {% endfor %}

    {{ form(form) }}
{% endblock %}
