{% extends '::base.html.twig' %}

{% block body -%}
    <ul class="breadcrumb">
        <li><a href="{{ path("forum") }}">Forum</a></li>
        <li class="active">{{ entity.title }}</li>
    </ul>

    <h1>
        <div class="btn-group pull-right">
            <a href="#" class="btn btn-default">
                <i class="fa fa-pencil fa-fw"></i>
            </a>
            <a href="#" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
                <li><a href="#">Radera tråd</a></li>
            </ul>
        </div>

        {{ entity.title }}
    </h1>

    <div class="list-group">
        <div class="list-group-item">
            <h4 class="list-group-item-heading">
                {{ entity.author }}
                <small>{{ entity.dateCreated|date("Y-m-d H:i") }}</small>
            </h4>
            <p class="list-group-item-text">Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
        </div>
        <div class="list-group-item">
            <h4 class="list-group-item-heading">{{ entity.author }} <small>{{ entity.dateCreated|date("Y-m-d H:i") }}</small></h4>
            <p class="list-group-item-text">Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
        </div>
        <div class="list-group-item">
            <h4 class="list-group-item-heading">{{ entity.author }} <small>{{ entity.dateCreated|date("Y-m-d H:i") }}</small></h4>
            <p class="list-group-item-text">Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
        </div>
    </div>

    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        {{ form(post_form) }}
    {% endif %}
{% endblock %}
